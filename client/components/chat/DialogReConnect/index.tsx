import { DialogContainer, Title } from './styles';

const DialogReConnect: React.FC = () => {
  return (
    <DialogContainer>
      <Title>채팅을 다시 시작하시겠어요?</Title>
    </DialogContainer>
  );
};

export default DialogReConnect;
